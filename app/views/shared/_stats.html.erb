<% @user ||= current_user %>

<div class="stat-box">
     <h2><%= @user.name %> | @<%= @user.username %></h2> 
    <span style="margin-right: 5%"><%= @user.following.count %><a href="<%= following_user_path(@user) %>"> following</a></span>

    <div id="followers" style="display: inline;"><%= @user.followers.count %><a href="<%= followers_user_path(@user) %>"></div> followers</a>

    <span style="margin-left: 5%"><%= @user.tracks.count %><a href="<%= user_path(@user) %>"> tracks</a></span>
    
    <span style="margin-left: 5%"><%= @user.albums.count %><a href="<%= user_path(@user) %>"> albums</a></span>
</div>