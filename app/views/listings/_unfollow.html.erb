<%= form_for(current_user.active_relationships.find_by(followed_id: @listing.user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn btn-block btn-danger" %>
<% end %>