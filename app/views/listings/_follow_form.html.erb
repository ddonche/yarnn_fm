<% if current_user != @listing.user %>
  <div id="follow_form">
    <% if current_user.following?(@listing.user) %>
      <%= render 'unfollow' %>
    <% else %>
      <%= render 'follow' %>
    <% end %>
  </div>
<% end %>