<%= form_for(current_user.active_relationships.find_by(followed_id: @track.user.id),
             html: { method: :delete },
             remote: true) do |f| %>
    <%= button_tag(type: 'submit', class: "btn btn-danger btn-block") do %>
      <i class="fas fa-user-times"></i> Unfollow
    <% end %>
<% end %>