<%= form_for(current_user.active_relationships.find_by(followed_id: @album.user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-sm btn-outline-danger" %>
<% end %>