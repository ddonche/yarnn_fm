<% if user_signed_in? && current_user.favorited?(@track) %>
  <%= link_to "Unfavorite", track_unfavorite_path(@track), method: :post, remote: true %>
<% else %>
  <%= link_to "Favorite", track_favorite_path(@track), method: :post, remote: true %>
<% end %>