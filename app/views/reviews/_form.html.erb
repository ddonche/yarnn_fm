<%= form_for [@listing, @review] do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div id="star-rating"></div>

  <div class="field">
    <%= f.label :content, class: "title" %>
    <%= f.text_area :content, cols: 87, rows: 4, autofocus: true, required: true %>
  </div>

  <div class="actions">
    <%= f.submit "Submit", class: "button button_highlight button_block" %>
  </div>
  
<% end %>

<script>
  $('#star-rating').raty({
    path: 'https://s3-us-west-2.amazonaws.com/yarnn/stars',
    scoreName: 'review[rating]'
  });
</script>
